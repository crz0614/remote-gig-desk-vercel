import type {Metadata} from "next";import "./globals.css";export const metadata:Metadata={title:"Relay — Multilingual Support Copilot",description:"Grounded multilingual support automation portfolio demo."};export default function Layout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
